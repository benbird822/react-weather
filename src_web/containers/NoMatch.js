import React, { Component } from 'react'
import { connect } from 'react-redux'

class NoMathch extends Component {
  constructor (props) {
    super(props)
  }

  render () {
      return (
        <h3>404!</h3>
      )
  }
}

export default NoMathch
